def create_properties_table(key,non_per_classes,per_classes,qm_results,out_file):
    # Import(s)
    import numpy as np
    import pandas as pd
    
    # Action
    
    key_df = pd.read_csv(key)
    key_ids = np.array(key_df['ID'])
    key_ras = np.array(key_df['RA'])
    key_decs = np.array(key_df['DEC'])
    
    non_per_classes_df = pd.read_
    non_per_file_names =    
    
    file_name = '/home/thaddaeus/Random/Playing with Astropy/Unfolded.png'
    
    # SORT IDS AT SOME POINT
    
    
    

non_periodic_classes = '' 
periodics_file = ''
qm_results = ''
key = ''
out_file = ''
create_properties_table(key,non_periodic_classes,periodics_file,qm_results,out_file)